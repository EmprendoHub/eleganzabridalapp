/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[

            "minio.salvawebpro.com",
            "lh3.googleusercontent.com",
        ],
    },
}

module.exports = nextConfig
